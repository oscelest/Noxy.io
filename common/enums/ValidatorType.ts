enum ValidatorType {
  INTEGER = "integer",
  FLOAT = "float",
  STRING = "string",
  BOOLEAN = "boolean",
  
  UUID = "uuid",
  DATE = "date",
  ENUM = "enum",
  EMAIL = "email",
  
  FILE = "file",
  PASSWORD = "password",
  ORDER = "sort-order",
}

export default ValidatorType;

